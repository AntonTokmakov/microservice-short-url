<?xml version="1.0" encoding="UTF-8" ?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <if condition='isDefined("http://192.168.31.171:3100")'>
        <then>
            <appender name="http://192.168.31.171:3100" class="com.github.loki4j.logback.Loki4jAppender">
                <http>
                    <url>${http://192.168.31.171:3100}/loki/api/v1/push</url>
                </http>
                <format class="">
                    <label>
                        <pattern>app=short-url-app,host=${HOSTNAME},level=%level</pattern>
                    </label>
                    <message>
                        <pattern>l=%level h=${HOSTNAME} c=%logger{20} t=%thread | %msg %ex</pattern>
                    </message>
                    <sortByTime>true</sortByTime>
                </format>
            </appender>

            <root level="info">
                <appender-ref ref="CONSOLE"/>
                <appender-ref ref="http://192.168.31.171:3100"/>
            </root>
        </then>
        <else>
            <root level="info">
                <appender-ref ref="CONSOLE"/>
            </root>
        </else>
    </if>

</configuration>